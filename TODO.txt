TODO / kontrolny zoznam
Aktualizovane: 2026-04-20

Hotove v aktualnom stave:
- Pipeline UI je presunute do `/settings/pipeline`.
- Samostatny nav link `Pipeline` bol odstráneny.
- Interny casovac, schedule API a `data/pipeline_schedules.json` boli odstranene.
- UI obsahuje navod na Windows Task Scheduler a Linux cron.
- Zastarane CLI prikazy boli odstranene z registrovanych Typer prikazov.
- Aktualny pipeline katalog pouziva zrozumitelne nazvy prikazov.

Aktualne rizika na sledovanie:
- Po realnom behu pipeline skontrolovat vzorku autorov flagovanych ako nejednoznacne iniciály alebo priezviska.
- Pri DB migracii v starsom prostredi overit, ze `setup-processing-queue` doplnil vsetky nove queue stlpce.
- Pri hromadnom `apply-journal-normalization` odporucam najprv spustit `--preview`.
- Externe planovane ulohy v Task Scheduler / cron logovat do samostatneho suboru.

Manualny smoke test:
1. Otvor `/settings/pipeline`.
2. Over, ze sa nezobrazuje casovanie ani zoznam naplanovanych behov.
3. Spusti status prikaz, napriklad `metadata-validation-status`.
4. Over, ze terminalovy vystup pride cez SSE.
